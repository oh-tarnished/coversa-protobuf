@0xffc6d0a14df06fe8;

# Code generated by protoc-gen-buffers. DO NOT EDIT.
# versions:
# 	protoc-gen-buffers dev
# 	protoc (unknown)
# source: protobuf/covesa/vss/motion/chassis_axle_wheel/v1/tire.proto
#
# target: capnp
# field slots below are recorded in buffers.lock; edit the .proto, not this file
#
# https://github.com/the-protobuf-project/buffers
using Cxx = import "/capnp/c++.capnp";
$Cxx.namespace("protobuf::covesa::vss::motion::chassis_axle_wheel::v1");

# Winter (cold-weather / snow) capability class of the tire currently fitted
# to this wheel.
#
# VSS: Vehicle.Chassis.Axle.Wheel.Tire.WinterStatus.
# (buffers.v1.enumeration).underlying asked for 32 bits. A Cap'n Proto enum is
# always 16, with no way to declare otherwise; the narrowing is honoured by
# the FlatBuffers target only.
enum WinterState @0x8abf167c943ac3ce {
  # Not specified.
  unspecified @0;
  # Not specified.
  unknown @1;
  # Non winter.
  nonWinter @2;
  # Winter.
  winter @3;
}

# Tire signals for wheel.
#
# VSS: Vehicle.Chassis.Axle.Wheel.Tire.
#
# Reference: COVESA Vehicle Signal Specification.
# https://covesa.github.io/vehicle_signal_specification/
struct Tire @0xdce78b57c4bc45c3 {
  # Tire pressure in kilo-Pascal.
  #
  # Unit: kPa. VSS: Vehicle.Chassis.Axle.Wheel.Tire.Pressure (sensor).
  pressure @0 :Int32;
  # Tire Pressure Status. True = Low tire pressure. False = Good tire
  # pressure.
  #
  # VSS: Vehicle.Chassis.Axle.Wheel.Tire.IsPressureLow (sensor).
  isPressureLow @1 :Bool;
  # Deprecated: v6.0 - use RubberTemperature or AirTemperature instead.
  #
  # Tire temperature in Celsius.
  #
  # Unit: Celsius. VSS: Vehicle.Chassis.Axle.Wheel.Tire.Temperature
  # (sensor).
  temperature @2 :Float64;
  # Rubber temperature of the tire in Celsius.
  #
  # Unit: Celsius. VSS: Vehicle.Chassis.Axle.Wheel.Tire.RubberTemperature
  # (sensor).
  rubberTemperature @3 :Float64;
  # Air temperature inside the tire in Celsius.
  #
  # Unit: Celsius. VSS: Vehicle.Chassis.Axle.Wheel.Tire.AirTemperature
  # (sensor).
  airTemperature @4 :Float64;
  # Winter (cold-weather / snow) capability class of the tire currently
  # fitted to this wheel.
  #
  # Describes the cold-weather grip capability of the fitted tire, which is
  # the primary determinant of available traction on snow and ice. Modelled
  # as an attribute because it is a property of the mounted equipment that
  # changes only when tires are swapped, not a continuously varying
  # measurement. WINTER indicates a dedicated winter / snow tire (e.g.
  # carrying the three-peak-mountain-snowflake 3PMSF marking), designed for
  # cold-weather rubber compounds and tread patterns that retain grip below
  # approximately 7 degrees Celsius and on snow / ice. NON_WINTER covers
  # summer and all-season tires alike: neither is a dedicated winter tire,
  # and from a snow-traction standpoint both leave the vehicle with
  # materially reduced cold-weather grip. The summer-vs-all-season
  # distinction is a longevity / comfort property that does not change the
  # snow-safety answer and is therefore intentionally not split here.
  # UNKNOWN shall be used when the fitted-tire winter capability cannot be
  # determined (e.g. no tire-identification data available, or input
  # confidence below threshold). Primary consumers: driver-warning systems
  # and navigation systems that adapt snow / mountain-pass routing and
  # advisory thresholds to the grip the vehicle actually has. Combined with
  # per-wheel placement, allows detection of mismatched or partially-fitted
  # winter tire sets. Criteria for each state may be OEM-, market-, or
  # jurisdiction-specific (e.g. national winter-tire regulations differ).
  # Related to VSS issue #877 (Connected Safety signals).
  #
  # VSS: Vehicle.Chassis.Axle.Wheel.Tire.WinterStatus (attribute).
  winterStatus @5 :WinterState;
}
