@0xa0aec9b5b8cc25c2;

# Code generated by protoc-gen-buffers. DO NOT EDIT.
# versions:
# 	protoc-gen-buffers dev
# 	protoc (unknown)
# source: protobuf/covesa/vss/propulsion/powertrain/v1/battery_conditioning.proto
#
# target: capnp
# field slots below are recorded in buffers.lock; edit the .proto, not this file
#
# https://github.com/the-protobuf-project/buffers
using Cxx = import "/capnp/c++.capnp";
$Cxx.namespace("protobuf::covesa::vss::propulsion::powertrain::v1");

using Wellknown = import "/buffers/wellknown.capnp";

# Defines requested mode for battery conditioning. INACTIVE - Battery
# conditioning inactive. FAST_CHARGING_PREPARATION - Battery conditioning
# for fast charging. DRIVING_PREPARATION - Battery conditioning for driving.
#
# VSS: Vehicle.Powertrain.TractionBattery.BatteryConditioning.RequestedMode.
# (buffers.v1.enumeration).underlying asked for 32 bits. A Cap'n Proto enum is
# always 16, with no way to declare otherwise; the narrowing is honoured by
# the FlatBuffers target only.
enum BatteryConditioningRequestedMode @0xc695bddd3450bfc4 {
  # Not specified.
  unspecified @0;
  # Inactive.
  inactive @1;
  # Fast charging preparation.
  fastChargingPreparation @2;
  # Driving preparation.
  drivingPreparation @3;
}

# Properties related to preparing the vehicle battery for charging or
# driving.
#
# VSS: Vehicle.Powertrain.TractionBattery.BatteryConditioning.
#
# Reference: COVESA Vehicle Signal Specification.
# https://covesa.github.io/vehicle_signal_specification/
struct BatteryConditioning @0xca0969dea9047e85 {
  # Indicates if battery conditioning is active (i.e. actively monitors
  # battery temperature). True = Active. False = Inactive.
  #
  # This signal is typically true when mode is not INACTIVE and time is
  # within defined start/end times.
  #
  # VSS: Vehicle.Powertrain.TractionBattery.BatteryConditioning.IsActive
  # (sensor).
  isActive @0 :Bool;
  # Indicating if battery conditioning is currently ongoing. Battery
  # conditioning is considered ongoing when the battery conditioning system
  # is actively heating or cooling the battery, or requesting heating or
  # cooling.
  #
  # When battery conditioning is active, but temperature is already within
  # acceptable range so that no cooling or heating is needed then IsOngoing
  # shall report False.
  #
  # VSS: Vehicle.Powertrain.TractionBattery.BatteryConditioning.IsOngoing
  # (sensor).
  isOngoing @1 :Bool;
  # Defines requested mode for battery conditioning. INACTIVE - Battery
  # conditioning inactive. FAST_CHARGING_PREPARATION - Battery conditioning
  # for fast charging. DRIVING_PREPARATION - Battery conditioning for
  # driving.
  #
  # The Mode and TargetTime can be used to calculate TargetTemperature and
  # StartTime
  #
  # VSS:
  # Vehicle.Powertrain.TractionBattery.BatteryConditioning.RequestedMode
  # (actuator).
  requestedMode @2 :BatteryConditioningRequestedMode;
  # Start time for battery conditioning, formatted according to ISO 8601
  # with UTC time zone.
  #
  # If the vehicle is asleep, this is the time the vehicle and the battery
  # conditioning system must wake up and start monitoring the battery and if
  # necessary start heating/cooling of the battery.
  #
  # Unit: iso8601. VSS:
  # Vehicle.Powertrain.TractionBattery.BatteryConditioning.StartTime
  # (actuator).
  startTime @3 :Wellknown.Timestamp;
  # Target time when conditioning shall be finished, formatted according to
  # ISO 8601 with UTC time zone.
  #
  # For FAST_CHARGING mode this is typically the time when charging is
  # supposed to start. For DRIVING mode this is typically the expected
  # departure time. Battery conditioning will be deactivated when this time
  # has passed.
  #
  # Unit: iso8601. VSS:
  # Vehicle.Powertrain.TractionBattery.BatteryConditioning.TargetTime
  # (actuator).
  targetTime @4 :Wellknown.Timestamp;
  # Target temperature for battery conditioning.
  #
  # Target temperature possibly differs between different modes as well as
  # other factors. Allowed deviation from target temperature is
  # implementation dependent.
  #
  # Unit: Celsius. VSS:
  # Vehicle.Powertrain.TractionBattery.BatteryConditioning.TargetTemperature
  # (actuator).
  targetTemperature @5 :Float64;
}
