@0x87f077388bb34aa2;

# Code generated by protoc-gen-buffers. DO NOT EDIT.
# versions:
# 	protoc-gen-buffers dev
# 	protoc (unknown)
# source: protobuf/covesa/vss/exterior/mirrors/v1/messages.proto
#
# target: capnp
# field slots below are recorded in buffers.lock; edit the .proto, not this file
#
# https://github.com/the-protobuf-project/buffers
using Cxx = import "/capnp/c++.capnp";
$Cxx.namespace("protobuf::covesa::vss::exterior::mirrors::v1");

using Mirrors = import "/protobuf/covesa/vss/exterior/mirrors/v1/mirrors.capnp";

# Request message for MirrorsService.GetMirrors, AIP-131
# <https://aip.dev/131>.
struct GetMirrorsRequest @0xe67c8afdf169d5ac {
  # Name of the Mirrors to retrieve, "vehicles/{vehicle}/mirrors/{mirrors}".
  # REQUIRED (AIP-203); Cap'n Proto cannot enforce it.
  name @0 :Text;
}

# Request message for MirrorsService.ListMirrors, AIP-132
# <https://aip.dev/132>.
struct ListMirrorsRequest @0xf7d12a2259ce12db {
  # The Vehicle whose mirrors to list, "vehicles/{vehicle}".
  # REQUIRED (AIP-203); Cap'n Proto cannot enforce it.
  parent @0 :Text;
  # Maximum mirrors to return. Defaults to 50, capped at 1000.
  pageSize @1 :Int32;
  # Page token from a previous ListMirrorsResponse.next_page_token.
  pageToken @2 :Text;
  # Filter expression, AIP-160 <https://aip.dev/160>.
  filter @3 :Text;
  # Include soft-deleted mirrors. AIP-164 <https://aip.dev/164>.
  showDeleted @4 :Bool;
}

# Response message for MirrorsService.ListMirrors, AIP-132
# <https://aip.dev/132>.
struct ListMirrorsResponse @0xda24d57b21b88791 {
  # The mirrors on this page.
  mirrors @0 :List(Mirrors.Mirrors);
  # Token for the next page, empty when this is the last page.
  nextPageToken @1 :Text;
}

# Request message for MirrorsService.CreateMirrors, AIP-133
# <https://aip.dev/133>.
struct CreateMirrorsRequest @0xffe766b97b2fe2cc {
  # The Vehicle to create the Mirrors under, "vehicles/{vehicle}".
  # REQUIRED (AIP-203); Cap'n Proto cannot enforce it.
  parent @0 :Text;
  # The Mirrors to create.
  # REQUIRED (AIP-203); Cap'n Proto cannot enforce it.
  mirrors @1 :Mirrors.Mirrors;
  # Client-assigned id, becoming the last segment of the resource name. The
  # server generates one when empty.
  mirrorsId @2 :Text;
}

# Request message for MirrorsService.UpdateMirrors, AIP-134
# <https://aip.dev/134>.
struct UpdateMirrorsRequest @0xf1672d146354f45c {
  # The Mirrors to update. Its name field identifies the resource.
  # REQUIRED (AIP-203); Cap'n Proto cannot enforce it.
  mirrors @0 :Mirrors.Mirrors;
  # Fields to update. An empty mask updates all mutable fields.
  #
  # Only an actuator is writable. A sensor or attribute named in the mask is
  # rejected: both are OUTPUT_ONLY, because a vehicle reports them and an
  # API cannot set them.
  updateMask @1 :List(Text);
}

# Request message for MirrorsService.DeleteMirrors, AIP-135
# <https://aip.dev/135>.
struct DeleteMirrorsRequest @0x9a6609c11aa0fa9b {
  # Name of the Mirrors to delete, "vehicles/{vehicle}/mirrors/{mirrors}".
  # REQUIRED (AIP-203); Cap'n Proto cannot enforce it.
  name @0 :Text;
}

# Request message for MirrorsService.UndeleteMirrors, AIP-164
# <https://aip.dev/164>.
struct UndeleteMirrorsRequest @0xeeb1b5869a11c070 {
  # Name of the soft-deleted Mirrors to restore,
  # "vehicles/{vehicle}/mirrors/{mirrors}".
  # REQUIRED (AIP-203); Cap'n Proto cannot enforce it.
  name @0 :Text;
}
