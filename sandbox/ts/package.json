{
  "name": "@oh-tarnished/coversa-protobuf-gen-typescript",
  "version": "0.0.0",
  "private": true,
  "description": "Compile-only project for the generated TypeScript, copied over gen/typescript in CI. Not published: it exists so a schema producing code tsc rejects is caught here rather than by the first consumer.",
  "license": "Apache-2.0",
  "type": "module",
  "dependencies": {
    "@bufbuild/protobuf": "^2.2.3"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
