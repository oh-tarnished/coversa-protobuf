// Copyright 2026 The Protobuf Project authors.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from the COVESA Vehicle Data Model,
// spec revision 2026-07-24 (36bc939). DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.covesa.vss.propulsion.powertrain.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "protobuf/covesa/vss/annotations/v1/annotations.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/fuel_system_types.proto";

option java_multiple_files = true;
option java_outer_classname = "FuelSystemProto";
option java_package = "io.github.theprotobufproject.protobuf.covesa.vss.propulsion.powertrain.v1";

// Fuel system data.
//
// VSS: Vehicle.Powertrain.FuelSystem.
//
// Reference: COVESA Vehicle Signal Specification.
// https://covesa.github.io/vehicle_signal_specification/
message FuelSystem {
  option (protobuf.covesa.vss.annotations.v1.branch) = {
    element: ELEMENT_BRANCH
    fqn: "Vehicle.Powertrain.FuelSystem"
  };

  // Current available fuel in the fuel tank expressed in liters.
  //
  // Unit: l. VSS: Vehicle.Powertrain.FuelSystem.AbsoluteLevel (sensor).
  double absolute_level = 1 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.AbsoluteLevel"
      unit: UNIT_LITER
      quantity_kind: QUANTITY_KIND_VOLUME
    }
  ];

  // Average fuel economy calculated from the most recent refueling event to
  // the current time.
  //
  // Unit: km/l. VSS: Vehicle.Powertrain.FuelSystem.AfterRefuelingFuelEconomy
  // (sensor).
  double current_tank_fuel_economy = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.AfterRefuelingFuelEconomy"
      unit: UNIT_KILOMETERS_PER_LITER
      quantity_kind: QUANTITY_KIND_DISTANCE_PER_VOLUME
    }
  ];

  // Average consumption in liters per 100 km.
  //
  // Unit: l/100km. VSS: Vehicle.Powertrain.FuelSystem.AverageConsumption
  // (sensor).
  double average_consumption = 3 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).double = {gte: 0.0},
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.AverageConsumption"
      unit: UNIT_LITER_PER_100_KILOMETERS
      quantity_kind: QUANTITY_KIND_VOLUME_PER_DISTANCE
    }
  ];

  // Fuel consumption since last refueling.
  //
  // Unit: l. VSS: Vehicle.Powertrain.FuelSystem.ConsumptionSinceLastRefuel
  // (sensor).
  double current_tank_consumption = 4 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.ConsumptionSinceLastRefuel"
      unit: UNIT_LITER
      quantity_kind: QUANTITY_KIND_VOLUME
    }
  ];

  // Fuel amount in liters consumed since start of current trip.
  //
  // Unit: l. VSS: Vehicle.Powertrain.FuelSystem.ConsumptionSinceStart
  // (sensor).
  double trip_consumption = 5 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.ConsumptionSinceStart"
      unit: UNIT_LITER
      quantity_kind: QUANTITY_KIND_VOLUME
    }
  ];

  // Cumulative average fuel economy calculated from vehicle start or last
  // user reset.
  //
  // Unit: km/l. VSS: Vehicle.Powertrain.FuelSystem.CumulativeFuelEconomy
  // (sensor).
  double cumulative_fuel_economy = 6 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.CumulativeFuelEconomy"
      unit: UNIT_KILOMETERS_PER_LITER
      quantity_kind: QUANTITY_KIND_DISTANCE_PER_VOLUME
    }
  ];

  // Average fuel economy for the current drive cycle, calculated from engine
  // start to current time.
  //
  // Unit: km/l. VSS: Vehicle.Powertrain.FuelSystem.DriveFuelEconomy
  // (sensor).
  double drive_fuel_economy = 7 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.DriveFuelEconomy"
      unit: UNIT_KILOMETERS_PER_LITER
      quantity_kind: QUANTITY_KIND_DISTANCE_PER_VOLUME
    }
  ];

  // Defines the hybrid type of the vehicle.
  //
  // VSS: Vehicle.Powertrain.FuelSystem.HybridType (attribute).
  FuelSystemHybridType hybrid_type = 8 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).enum.defined_only = true,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.FuelSystem.HybridType"
    }
  ];

  // Current consumption in liters per 100 km.
  //
  // Unit: l/100km. VSS: Vehicle.Powertrain.FuelSystem.InstantConsumption
  // (sensor).
  double instant_consumption = 9 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).double = {gte: 0.0},
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.InstantConsumption"
      unit: UNIT_LITER_PER_100_KILOMETERS
      quantity_kind: QUANTITY_KIND_VOLUME_PER_DISTANCE
    }
  ];

  // Real-time instantaneous fuel economy calculated over a short time
  // window.
  //
  // Unit: km/l. VSS: Vehicle.Powertrain.FuelSystem.InstantantFuelEconomy
  // (sensor).
  double instantant_fuel_economy = 10 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.InstantantFuelEconomy"
      unit: UNIT_KILOMETERS_PER_LITER
      quantity_kind: QUANTITY_KIND_DISTANCE_PER_VOLUME
    }
  ];

  // Indicates whether eco start stop is currently enabled.
  //
  // VSS: Vehicle.Powertrain.FuelSystem.IsEngineStopStartEnabled (sensor).
  bool is_engine_stop_start_enabled = 11 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.IsEngineStopStartEnabled"
    }
  ];

  // Indicates that the fuel gauge on the instrument cluster displays empty
  // (0).
  //
  // VSS: Vehicle.Powertrain.FuelSystem.IsFuelLevelEmpty (sensor).
  bool is_fuel_level_empty = 12 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.IsFuelLevelEmpty"
    }
  ];

  // Indicates that the fuel level is low (e.g. <50km range).
  //
  // VSS: Vehicle.Powertrain.FuelSystem.IsFuelLevelLow (sensor).
  bool is_fuel_level_low = 13 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.IsFuelLevelLow"
    }
  ];

  // Status of the fuel port flap(s). True if at least one is open.
  //
  // VSS: Vehicle.Powertrain.FuelSystem.IsFuelPortFlapOpen (actuator).
  bool is_fuel_port_flap_open = 14 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.Powertrain.FuelSystem.IsFuelPortFlapOpen"
    }
  ];

  // Remaining range in meters using only liquid fuel.
  //
  // Unit: m. VSS: Vehicle.Powertrain.FuelSystem.Range (sensor).
  int64 range_control = 15 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int64 = {
      gte: 0
      lte: 4294967295
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.Range"
      unit: UNIT_METER
      quantity_kind: QUANTITY_KIND_LENGTH
    }
  ];

  // Position of refuel port(s). First part indicates side of vehicle, second
  // part relative position on that side.
  //
  // VSS: Vehicle.Powertrain.FuelSystem.RefuelPortPosition (attribute).
  FuelSystemRefuelPortPosition refuel_port_position = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).enum.defined_only = true,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.FuelSystem.RefuelPortPosition"
    }
  ];

  // Level in fuel tank as percent of capacity. 0 = empty. 100 = full.
  //
  // Unit: percent. VSS: Vehicle.Powertrain.FuelSystem.RelativeLevel
  // (sensor).
  int32 relative_level = 17 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 100
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.RelativeLevel"
      unit: UNIT_PERCENT
      quantity_kind: QUANTITY_KIND_RELATION
    }
  ];

  // Detailed information on fuels supported by the vehicle. Identifiers
  // originating from DIN EN 16942:2021-08, appendix B, with additional
  // suffix for octane (RON) where relevant.
  //
  // VSS: Vehicle.Powertrain.FuelSystem.SupportedFuel (attribute).
  FuelSystemSupportedFuel supported_fuel = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).enum.defined_only = true,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.FuelSystem.SupportedFuel"
    }
  ];

  // High level information of fuel types supported
  //
  // VSS: Vehicle.Powertrain.FuelSystem.SupportedFuelTypes (attribute).
  FuelSystemSupportedFuelTypes supported_fuel_types = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).enum.defined_only = true,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.FuelSystem.SupportedFuelTypes"
    }
  ];

  // Capacity of the fuel tank in liters.
  //
  // Unit: l. VSS: Vehicle.Powertrain.FuelSystem.TankCapacity (attribute).
  double tank_capacity = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.FuelSystem.TankCapacity"
      unit: UNIT_LITER
      quantity_kind: QUANTITY_KIND_VOLUME
    }
  ];

  // Time remaining in seconds before the fuel tank is empty.
  //
  // Unit: s. VSS: Vehicle.Powertrain.FuelSystem.TimeRemaining (sensor).
  int64 time_remaining = 21 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int64 = {
      gte: 0
      lte: 4294967295
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.FuelSystem.TimeRemaining"
      unit: UNIT_SECOND
      quantity_kind: QUANTITY_KIND_DURATION
    }
  ];
}
