// Copyright 2026 The Protobuf Project authors.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from the COVESA Vehicle Signal Specification,
// revision 2026-09-02 (cd4bc50), and the
// Vehicle Data Model, revision 2026-07-24 (36bc939).
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.covesa.vss.propulsion.powertrain.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "protobuf/covesa/vss/annotations/v1/annotations.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/battery_conditioning.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/cell_voltage.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/charging.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/date.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/dcdc.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/state_of_charge.proto";
import "protobuf/covesa/vss/propulsion/powertrain/v1/temperature.proto";

option java_multiple_files = true;
option java_outer_classname = "TractionBatteryProto";
option java_package = "io.github.ohtarnished.protobuf.covesa.vss.propulsion.powertrain.v1";

// Battery Management data.
//
// VSS: Vehicle.Powertrain.TractionBattery.
//
// Reference: COVESA Vehicle Signal Specification.
// https://covesa.github.io/vehicle_signal_specification/
message TractionBattery {
  option (protobuf.covesa.vss.annotations.v1.branch) = {
    element: ELEMENT_BRANCH
    fqn: "Vehicle.Powertrain.TractionBattery"
  };

  // Battery Identification Number as assigned by OEM.
  //
  // This could be serial number, part number plus serial number, UUID, or
  // any other identifier that the OEM want to use to uniquely identify the
  // battery individual.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.Id (attribute).
  string id = 1 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.TractionBattery.Id"
    }
  ];

  // Production date of battery in ISO8601 format, e.g. YYYY-MM-DD.
  //
  // Unit: iso8601. VSS: Vehicle.Powertrain.TractionBattery.ProductionDate
  // (attribute).
  Date production = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.TractionBattery.ProductionDate"
      unit: UNIT_ISO8601
      quantity_kind: QUANTITY_KIND_DATETIME
    }
  ];

  // Current error codes related to the battery, if any.
  //
  // Error code format is not defined, it may be DTCs according to OBD II
  // (SAE-J2012DA_201812) standard ([P|C|B|U]XXXXX ) or any other format.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.ErrorCodes (sensor).
  repeated string error_codes = 3 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.ErrorCodes"
    }
  ];

  // Indicating if the power (positive terminator) of the traction battery is
  // connected to the powertrain.
  //
  // It might be possible to disconnect the traction battery used by an
  // electric powertrain. This is achieved by connectors, typically one for
  // plus and one for minus.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.IsPowerConnected (sensor).
  bool is_power_connected = 4 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.IsPowerConnected"
    }
  ];

  // Indicating if the ground (negative terminator) of the traction battery
  // is connected to the powertrain.
  //
  // It might be possible to disconnect the traction battery used by an
  // electric powertrain. This is achieved by connectors, typically one for
  // plus and one for minus.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.IsGroundConnected (sensor).
  bool is_ground_connected = 5 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.IsGroundConnected"
    }
  ];

  // Temperature Information for the battery pack.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.Temperature.
  Temperature temperature = 6 [(google.api.field_behavior) = OPTIONAL];

  // Voltage information for cells in the battery pack.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.CellVoltage.
  CellVoltage cell_voltage = 7 [(google.api.field_behavior) = OPTIONAL];

  // Gross capacity of the battery.
  //
  // Unit: kWh. VSS: Vehicle.Powertrain.TractionBattery.GrossCapacity
  // (attribute).
  int32 gross_capacity = 8 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 65535
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.TractionBattery.GrossCapacity"
      unit: UNIT_KWH
      quantity_kind: QUANTITY_KIND_WORK
    }
  ];

  // Total net capacity of the battery considering aging.
  //
  // Unit: kWh. VSS: Vehicle.Powertrain.TractionBattery.NetCapacity (sensor).
  int32 net_capacity = 9 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 65535
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.NetCapacity"
      unit: UNIT_KWH
      quantity_kind: QUANTITY_KIND_WORK
    }
  ];

  // Calculated battery state of health at standard conditions.
  //
  // Exact formula is implementation dependent. Could be e.g. current
  // capacity at 20 degrees Celsius divided with original capacity at the
  // same temperature.
  //
  // Unit: percent. VSS: Vehicle.Powertrain.TractionBattery.StateOfHealth
  // (sensor).
  double health_state = 10 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).double = {
      gte: 0.0
      lte: 100.0
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.StateOfHealth"
      unit: UNIT_PERCENT
      quantity_kind: QUANTITY_KIND_RELATION
    }
  ];

  // Information on the state of charge of the vehicle's high voltage
  // battery.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.StateOfCharge.
  StateOfCharge charge_state = 11 [(google.api.field_behavior) = OPTIONAL];

  // Nominal Voltage of the battery.
  //
  // Nominal voltage typically refers to voltage of fully charged battery
  // when delivering rated capacity.
  //
  // Unit: V. VSS: Vehicle.Powertrain.TractionBattery.NominalVoltage
  // (attribute).
  int32 nominal_voltage = 12 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 65535
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.TractionBattery.NominalVoltage"
      unit: UNIT_V
      quantity_kind: QUANTITY_KIND_VOLTAGE
    }
  ];

  // Max allowed voltage of the battery, e.g. during charging.
  //
  // Unit: V. VSS: Vehicle.Powertrain.TractionBattery.MaxVoltage (attribute).
  int32 max_voltage = 13 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 65535
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.TractionBattery.MaxVoltage"
      unit: UNIT_V
      quantity_kind: QUANTITY_KIND_VOLTAGE
    }
  ];

  // Current Voltage of the battery.
  //
  // Unit: V. VSS: Vehicle.Powertrain.TractionBattery.CurrentVoltage
  // (sensor).
  double current_voltage = 14 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.CurrentVoltage"
      unit: UNIT_V
      quantity_kind: QUANTITY_KIND_VOLTAGE
    }
  ];

  // Current current flowing in/out of battery. Positive = Current flowing in
  // to battery, e.g. during charging. Negative = Current flowing out of
  // battery, e.g. during driving.
  //
  // Unit: A. VSS: Vehicle.Powertrain.TractionBattery.CurrentCurrent
  // (sensor).
  double current_current = 15 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.CurrentCurrent"
      unit: UNIT_A
      quantity_kind: QUANTITY_KIND_ELECTRIC_CURRENT
    }
  ];

  // Current electrical energy flowing in/out of battery. Positive = Energy
  // flowing in to battery, e.g. during charging. Negative = Energy flowing
  // out of battery, e.g. during driving.
  //
  // Unit: W. VSS: Vehicle.Powertrain.TractionBattery.CurrentPower (sensor).
  double current_power = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.CurrentPower"
      unit: UNIT_W
      quantity_kind: QUANTITY_KIND_POWER
    }
  ];

  // The accumulated energy delivered to the battery during charging over
  // lifetime of the battery.
  //
  // Unit: kWh. VSS:
  // Vehicle.Powertrain.TractionBattery.AccumulatedChargedEnergy (sensor).
  double accumulated_charged_energy = 17 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.AccumulatedChargedEnergy"
      unit: UNIT_KWH
      quantity_kind: QUANTITY_KIND_WORK
    }
  ];

  // The accumulated energy leaving HV battery for propulsion and auxiliary
  // loads over lifetime of the battery.
  //
  // Unit: kWh. VSS:
  // Vehicle.Powertrain.TractionBattery.AccumulatedConsumedEnergy (sensor).
  double accumulated_consumed_energy = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.AccumulatedConsumedEnergy"
      unit: UNIT_KWH
      quantity_kind: QUANTITY_KIND_WORK
    }
  ];

  // The accumulated charge throughput delivered to the battery during
  // charging over lifetime of the battery.
  //
  // Unit: Ah. VSS:
  // Vehicle.Powertrain.TractionBattery.AccumulatedChargedThroughput
  // (sensor).
  double accumulated_charged_throughput = 19 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.AccumulatedChargedThroughput"
      unit: UNIT_AH
      quantity_kind: QUANTITY_KIND_ELECTRIC_CHARGE
    }
  ];

  // The accumulated charge throughput leaving HV battery for propulsion and
  // auxiliary loads over lifetime of the battery.
  //
  // Unit: Ah. VSS:
  // Vehicle.Powertrain.TractionBattery.AccumulatedConsumedThroughput
  // (sensor).
  double accumulated_consumed_throughput = 20 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.AccumulatedConsumedThroughput"
      unit: UNIT_AH
      quantity_kind: QUANTITY_KIND_ELECTRIC_CHARGE
    }
  ];

  // Electrical energy lost by power dissipation to heat inside the battery.
  //
  // Unit: W. VSS: Vehicle.Powertrain.TractionBattery.PowerLoss (sensor).
  double power_loss = 21 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.PowerLoss"
      unit: UNIT_W
      quantity_kind: QUANTITY_KIND_POWER
    }
  ];

  // Remaining range in meters using only battery.
  //
  // Unit: m. VSS: Vehicle.Powertrain.TractionBattery.Range (sensor).
  int64 range_control = 22 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int64 = {
      gte: 0
      lte: 4294967295
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.Range"
      unit: UNIT_M
      quantity_kind: QUANTITY_KIND_LENGTH
    }
  ];

  // Time remaining in seconds before the battery is empty.
  //
  // Unit: s. VSS: Vehicle.Powertrain.TractionBattery.TimeRemaining (sensor).
  int64 time_remaining = 23 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int64 = {
      gte: 0
      lte: 4294967295
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.TimeRemaining"
      unit: UNIT_S
      quantity_kind: QUANTITY_KIND_DURATION
    }
  ];

  // Properties related to battery charging.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.Charging.
  Charging charging = 24 [(google.api.field_behavior) = OPTIONAL];

  // Properties related to DC/DC converter converting high voltage (from high
  // voltage battery) to vehicle low voltage (supply voltage, typically 12
  // Volts).
  //
  // VSS: Vehicle.Powertrain.TractionBattery.DCDC.
  Dcdc dcdc = 25 [(google.api.field_behavior) = OPTIONAL];

  // Properties related to preparing the vehicle battery for charging or
  // driving.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.BatteryConditioning.
  BatteryConditioning battery_conditioning = 26 [(google.api.field_behavior) = OPTIONAL];
}
