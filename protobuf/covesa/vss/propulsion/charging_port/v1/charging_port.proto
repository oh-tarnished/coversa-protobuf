// Copyright 2026 Srikanth Kandarpa.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from the COVESA Vehicle Signal Specification,
// revision 2026-09-02 (cd4bc50), and the
// Vehicle Data Model, revision 2026-07-24 (36bc939).
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.covesa.vss.propulsion.charging_port.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "google/api/field_info.proto";
import "google/api/resource.proto";
import "google/protobuf/timestamp.proto";
import "protobuf/covesa/vss/annotations/v1/annotations.proto";

option java_multiple_files = true;
option java_outer_classname = "ChargingPortProto";
option java_package = "io.github.ohtarnished.protobuf.covesa.vss.propulsion.charging_port.v1";

// Properties related to a particular charging port available in the vehicle.
//
// If a vehicle has a single charging port, then use the instance
// AnyPosition.
//
// VSS: Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.
//
// Instanced: FrontLeft, FrontMiddle, FrontRight, RearLeft, RearMiddle,
// RearRight, AnyPosition. The axes together name one occurrence, which is
// why each is a resource of its own rather than a repeated field.
//
// A child resource of Powertrain, named
// "vehicles/{vehicle}/chargingPorts/{charging_port}". Its parent is a
// declared resource in protobuf.covesa.vss.propulsion.powertrain.v1, which
// is what makes this pattern legal rather than a reference to a collection
// nothing can create.
//
// Reference: COVESA Vehicle Signal Specification.
// https://covesa.github.io/vehicle_signal_specification/
message ChargingPort {
  option (google.api.resource) = {
    type: "vdm.covesa.org/ChargingPort"
    pattern: "vehicles/{vehicle}/chargingPorts/{charging_port}"
    singular: "chargingPort"
    plural: "chargingPorts"
  };

  // Resource name, "vehicles/{vehicle}/chargingPorts/{charging_port}".
  // Assigned by the server.
  string name = 1 [
    (google.api.field_behavior) = IDENTIFIER,
    (buf.validate.field).string.pattern = "^vehicles/[^/]+/chargingPorts/[^/]+$",
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  // Server-assigned unique id.
  string uid = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (google.api.field_info).format = UUID4,
    (buf.validate.field).string.uuid = true,
    (buf.validate.field).ignore = IGNORE_IF_ZERO_VALUE
  ];

  reserved 8 to 15;

  // Weak validator for optimistic concurrency, AIP-154
  // <https://aip.dev/154>.
  string etag = 3 [(google.api.field_behavior) = OUTPUT_ONLY];

  // When this resource was soft-deleted, unset while it is live. AIP-164
  // <https://aip.dev/164>.
  google.protobuf.Timestamp delete_time = 4 [(google.api.field_behavior) = OUTPUT_ONLY];

  // When a soft-deleted resource is purged for good. AIP-164
  // <https://aip.dev/164>.
  google.protobuf.Timestamp expire_time = 5 [(google.api.field_behavior) = OUTPUT_ONLY];

  // When this resource was created.
  google.protobuf.Timestamp create_time = 6 [(google.api.field_behavior) = OUTPUT_ONLY];

  // When this resource was last modified.
  google.protobuf.Timestamp update_time = 7 [(google.api.field_behavior) = OUTPUT_ONLY];

  option (protobuf.covesa.vss.annotations.v1.branch) = {
    element: ELEMENT_BRANCH
    fqn: "Vehicle.Powertrain.TractionBattery.Charging.ChargingPort"
  };

  // A list of the supported (i.e., available) charging inlets in a
  // particular charging port. IEC types refer to IEC 62196, GBT refers to
  // GB/T 20234.
  //
  // A vehicle may have multiple charging ports. IEC_TYPE_1_AC refers to Type
  // 1 as defined in IEC 62196-2. Also known as Yazaki or J1772 connector.
  // IEC_TYPE_2_AC refers to Type 2 as defined in IEC 62196-2. Also known as
  // Mennekes connector. IEC_TYPE_3_AC refers to Type 3 as defined in IEC
  // 62196-2. Also known as Scame connector. IEC_TYPE_4_DC refers to AA
  // configuration as defined in IEC 62196-3. Also known as Type 4 or CHAdeMO
  // connector. IEC_TYPE_1_CCS_DC refers to EE Configuration as defined in
  // IEC 62196-3. Also known as CCS1 or Combo1 connector. IEC_TYPE_2_CCS_DC
  // refers to FF Configuration as defined in IEC 62196-3. Also known as CCS2
  // or Combo2 connector. TESLA_ROADSTER, TESLA_HPWC (High Power Wall
  // Connector) and TESLA_SUPERCHARGER refer to non-standardized charging
  // ports/methods used by Tesla. GBT_AC refers to connector specified in
  // GB/T 20234.2. GBT_DC refers to connector specified in GB/T 20234.3. Also
  // specified as BB Configuration in IEC 62196-3. OTHER shall be used for
  // charging ports not included in the list above. For additional
  // information see https://en.wikipedia.org/wiki/IEC_62196.
  //
  // VSS:
  // Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.SupportedInletTypes
  // (attribute).
  repeated SupportedInletTypes supported_inlet_types = 16 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).enum.defined_only = true,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ATTRIBUTE
      fqn: "Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.SupportedInletTypes"
    }
  ];

  // Status of the charging port flap(s).
  //
  // True = at least one flap of this port is open, False = All flaps of this
  // port are closed.
  //
  // VSS: Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.IsFlapOpen
  // (actuator).
  bool is_flap_open = 17 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.IsFlapOpen"
    }
  ];

  // Indicates whether a charging cable is physically connected to a
  // particular charging port or not.
  //
  // VSS:
  // Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.IsChargingCableConnected
  // (sensor).
  bool is_charging_cable_connected = 18 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.IsChargingCableConnected"
    }
  ];

  // Is charging cable locked to prevent removal.
  //
  // Locking of charging cable can be used to prevent unintentional removing
  // during charging.
  //
  // VSS:
  // Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.IsChargingCableLocked
  // (actuator).
  bool is_charging_cable_locked = 19 [
    (google.api.field_behavior) = OPTIONAL,
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.IsChargingCableLocked"
    }
  ];
}
// A list of the supported (i.e., available) charging inlets in a particular
// charging port. IEC types refer to IEC 62196, GBT refers to GB/T 20234.
//
// VSS:
// Vehicle.Powertrain.TractionBattery.Charging.ChargingPort.SupportedInletTypes.
enum SupportedInletTypes {
  // Not specified.
  SUPPORTED_INLET_TYPES_UNSPECIFIED = 0;

  // Iec type 1 ac.
  SUPPORTED_INLET_TYPES_IEC_TYPE_1_AC = 1;

  // Iec type 2 ac.
  SUPPORTED_INLET_TYPES_IEC_TYPE_2_AC = 2;

  // Iec type 3 ac.
  SUPPORTED_INLET_TYPES_IEC_TYPE_3_AC = 3;

  // Iec type 4 dc.
  SUPPORTED_INLET_TYPES_IEC_TYPE_4_DC = 4;

  // Iec type 1 ccs dc.
  SUPPORTED_INLET_TYPES_IEC_TYPE_1_CCS_DC = 5;

  // Iec type 2 ccs dc.
  SUPPORTED_INLET_TYPES_IEC_TYPE_2_CCS_DC = 6;

  // Tesla roadster.
  SUPPORTED_INLET_TYPES_TESLA_ROADSTER = 7;

  // Tesla hpwc.
  SUPPORTED_INLET_TYPES_TESLA_HPWC = 8;

  // Tesla supercharger.
  SUPPORTED_INLET_TYPES_TESLA_SUPERCHARGER = 9;

  // Gbt ac.
  SUPPORTED_INLET_TYPES_GBT_AC = 10;

  // Gbt dc.
  SUPPORTED_INLET_TYPES_GBT_DC = 11;

  // Other.
  SUPPORTED_INLET_TYPES_OTHER = 12;
}
