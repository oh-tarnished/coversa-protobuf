// Copyright 2026 Srikanth Kandarpa.
// SPDX-License-Identifier: Apache-2.0
//
// Generated by sync from the COVESA Vehicle Signal Specification,
// revision 2026-09-02 (cd4bc50), and the
// Vehicle Data Model, revision 2026-07-24 (36bc939).
// DO NOT EDIT.
//
// Regenerate with: just sync
// Revision history: docs/spec.md

syntax = "proto3";

package protobuf.covesa.vss.motion.motion_management.v1;

import "buf/validate/validate.proto";
import "google/api/field_behavior.proto";
import "protobuf/covesa/vss/annotations/v1/annotations.proto";

option java_multiple_files = true;
option java_outer_classname = "SteeringWheelProto";
option java_package = "io.github.ohtarnished.protobuf.covesa.vss.motion.motion_management.v1";

// MotionManagement related to steering wheel.
//
// VSS: Vehicle.MotionManagement.Steering.SteeringWheel.
//
// Reference: COVESA Vehicle Signal Specification.
// https://covesa.github.io/vehicle_signal_specification/
message SteeringWheel {
  option (protobuf.covesa.vss.annotations.v1.branch) = {
    element: ELEMENT_BRANCH
    fqn: "Vehicle.MotionManagement.Steering.SteeringWheel"
  };

  // Represents the current input angle of the steering system, typically
  // corresponds to angle applied by driver on the steering-wheel. Positive
  // for angle in counterclockwise direction (based on ISO8855).
  //
  // Unit: degrees. VSS:
  // Vehicle.MotionManagement.Steering.SteeringWheel.Angle (sensor).
  int32 angle = 1 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int32 = {
      gte: -32768
      lte: 32767
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.Angle"
      unit: UNIT_DEGREES
      quantity_kind: QUANTITY_KIND_ANGLE
    }
  ];

  // Steering-wheel angle request to the steering actuator (external
  // set-point for steer-by-wire). Positive sign for angle in
  // counterclockwise direction (based on ISO8855).
  //
  // Unit: degrees. VSS:
  // Vehicle.MotionManagement.Steering.SteeringWheel.AngleTarget (actuator).
  int32 angle_target = 2 [
    (google.api.field_behavior) = OPTIONAL,
    (buf.validate.field).int32 = {
      gte: -32768
      lte: 32767
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.AngleTarget"
      unit: UNIT_DEGREES
      quantity_kind: QUANTITY_KIND_ANGLE
    }
  ];

  // Mode used for controlling Steering-wheel angle interface. 0 indicates
  // interface disabled. Other values activate vehicle specific modes.
  //
  // VSS: Vehicle.MotionManagement.Steering.SteeringWheel.AngleTargetMode
  // (actuator).
  int32 angle_target_mode = 3 [
    (google.api.field_behavior) = OPTIONAL,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 255
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.AngleTargetMode"
    }
  ];

  // Steering-wheel torque offset request to the steering actuator, added to
  // the actuator internal calculated target value. Positive sign for torque
  // in counterclockwise direction (based on ISO8855).
  //
  // Unit: Nm. VSS:
  // Vehicle.MotionManagement.Steering.SteeringWheel.TorqueOffsetTarget
  // (actuator).
  int32 torque_offset_target = 4 [
    (google.api.field_behavior) = OPTIONAL,
    (buf.validate.field).int32 = {
      gte: -32768
      lte: 32767
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.TorqueOffsetTarget"
      unit: UNIT_NM
      quantity_kind: QUANTITY_KIND_TORQUE
    }
  ];

  // Mode used for controlling steering-wheel torque offset interface. 0
  // indicates interface disabled. Other values activate vehicle specific
  // modes.
  //
  // VSS:
  // Vehicle.MotionManagement.Steering.SteeringWheel.TorqueOffsetTargetMode
  // (actuator).
  int32 torque_offset_target_mode = 5 [
    (google.api.field_behavior) = OPTIONAL,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 255
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.TorqueOffsetTargetMode"
    }
  ];

  // Steering-wheel torque request to the steering actuator (external
  // set-point for steer-by-wire). Positive sign for torque in
  // counterclockwise direction (based on ISO8855).
  //
  // Unit: Nm. VSS:
  // Vehicle.MotionManagement.Steering.SteeringWheel.TorqueTarget (actuator).
  int32 torque_target = 6 [
    (google.api.field_behavior) = OPTIONAL,
    (buf.validate.field).int32 = {
      gte: -32768
      lte: 32767
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.TorqueTarget"
      unit: UNIT_NM
      quantity_kind: QUANTITY_KIND_TORQUE
    }
  ];

  // Mode used for controlling Steering-wheel torque interface. 0 indicates
  // interface disabled. Other values activate vehicle specific modes.
  //
  // VSS: Vehicle.MotionManagement.Steering.SteeringWheel.TorqueTargetMode
  // (actuator).
  int32 torque_target_mode = 7 [
    (google.api.field_behavior) = OPTIONAL,
    (buf.validate.field).int32 = {
      gte: 0
      lte: 255
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_ACTUATOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.TorqueTargetMode"
    }
  ];

  // Represents the current input torque for steering system, typically
  // corresponds to torque applied by driver on the steering wheel. Positive
  // for torque in counterclockwise direction (based on ISO8855).
  //
  // Unit: Nm. VSS: Vehicle.MotionManagement.Steering.SteeringWheel.Torque
  // (sensor).
  int32 torque = 8 [
    (google.api.field_behavior) = OUTPUT_ONLY,
    (buf.validate.field).int32 = {
      gte: -32768
      lte: 32767
    },
    (protobuf.covesa.vss.annotations.v1.signal) = {
      element: ELEMENT_SENSOR
      fqn: "Vehicle.MotionManagement.Steering.SteeringWheel.Torque"
      unit: UNIT_NM
      quantity_kind: QUANTITY_KIND_TORQUE
    }
  ];
}
