# AmbientLight

**Package** `protobuf.covesa.vss.interior.ambient_light.v1`  
**Resource** `vehicles/{vehicle}/ambientLights/{ambient_light}`  
**Shape** child collection — full CRUD plus undelete

> [!IMPORTANT]
> Auto-generated by `sync docs` from the COVESA Vehicle Data Model,
> spec revision `2026-07-24` (`36bc939`).
> Do not edit by hand; run `just docs`.

Decorative coloured light inside the cabin, usually mounted on the door, ceiling, etc.

## Service `AmbientLights`

| Method | Request | Response | Summary |
| --- | --- | --- | --- |
| `GetAmbientLight` | `GetAmbientLightRequest` | `AmbientLight` | Retrieves one AmbientLight. `GET /v1/{name=vehicles/*/ambientLights/*}` |
| `ListAmbientLights` | `ListAmbientLightsRequest` | `ListAmbientLightsResponse` | Lists them, paginated per [AIP-158](https://aip.dev/158). `GET /v1/{parent=vehicles/*}/ambientLights` |
| `CreateAmbientLight` | `CreateAmbientLightRequest` | `AmbientLight` | Creates one. `POST /v1/{parent=vehicles/*}/ambientLights` |
| `UpdateAmbientLight` | `UpdateAmbientLightRequest` | `AmbientLight` | Partial update; only an actuator is writable. `PATCH` |
| `DeleteAmbientLight` | `DeleteAmbientLightRequest` | `google.protobuf.Empty` | Soft delete; recoverable until `expire_time`. `DELETE` |
| `UndeleteAmbientLight` | `UndeleteAmbientLightRequest` | `AmbientLight` | Restores a soft-deleted AmbientLight. `POST …:undelete` |

## Messages

### `AmbientLight`

Decorative coloured light inside the cabin, usually mounted on the door, ceiling, etc.

Carries the AIP identity and lifecycle fields — `name`, `uid`, `etag` and the four timestamps — ahead of the signals below. Field numbers 8–15 are reserved.

| Field | Type | Behavior | Unit | Description |
| --- | --- | --- | --- | --- |
| `color` | `string` | `OPTIONAL` | — | Hexadecimal color code represented as a 3-byte RGB (i.e. Red, Green, and Blue) value preceded by a hash symbol "#". Allowed range "#000000" to "#FFFFFF". |
| `intensity` | `int32` | `OPTIONAL` | `percent` | How much of the maximum possible brightness of the light is used. 1 = Maximum attenuation, 100 = No attenuation (i.e. full brightness). |
| `is_light_on` | `bool` | `OPTIONAL` | — | Indicates whether the light is turned on. True = On, False = Off. |
| `instance_tag` | `AmbientLightInstanceTag` | `OPTIONAL` | — | Which instance of this branch the values belong to. |

### `AmbientLightInstanceTag`

AmbientLightInstanceTag is a node of the COVESA Vehicle Signal Specification.

| Field | Type | Behavior | Unit | Description |
| --- | --- | --- | --- | --- |
| `dimension1` | `Dimension1` | `OPTIONAL` | — | Instance axis 1. VSS expands a branch across each axis in turn, so the axes together name one instance. |
| `dimension2` | `Dimension2` | `OPTIONAL` | — | Instance axis 2. VSS expands a branch across each axis in turn, so the axes together name one instance. |

## Enums

### `Dimension1`

Dimensional enum for VSS instance dimension 1.

| Value | Description |
| --- | --- |
| `DIMENSION1_ROW1` | Row1. |
| `DIMENSION1_ROW2` | Row2. |

### `Dimension2`

Dimensional enum for VSS instance dimension 2.

| Value | Description |
| --- | --- |
| `DIMENSION2_DRIVER_SIDE` | Driver side. |
| `DIMENSION2_PASSENGER_SIDE` | Passenger side. |

